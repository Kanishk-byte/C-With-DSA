#include <iostream>
using namespace std;

int main()
{

    int x = 10, y = 20;

    if (x == y); // Semicolon means end of statement.
    cout << x << " " << y; // This line is not included in if condition because of semicolon(;).

    return 0;
}
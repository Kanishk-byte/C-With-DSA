#include <iostream>
using namespace std;

// * Value of x & a.

int main()
{

    char x;
    int a = 2;
    x = (a > 0) ? 'a' : 'S';
    cout << "Value of x : " << x << endl;
    cout << "Value of a : " << a << endl;

    return 0;
}
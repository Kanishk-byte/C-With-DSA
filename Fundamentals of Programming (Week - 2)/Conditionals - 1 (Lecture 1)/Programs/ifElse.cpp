#include <iostream>
using namespace std;

// * In if - else only one statement is run.

int main()
{

    /*
    if ( condition )
    {
        code -> Statement
    } else {
        code -> Statement
    }
    */

    return 0;
}
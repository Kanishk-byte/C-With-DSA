#include <iostream>
using namespace std;

// * Check number is positive or negative.

int main()
{

    int number = 0;

    if (number > 0)
        cout << "Number is positive";
    if (number == 0)
        cout << "Number is not negative";

    return 0;
}
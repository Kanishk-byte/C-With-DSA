#include <iostream>
using namespace std;

// * How many of these can be a variable name.

int main()
{

    // * 01Pwskills = Not a Variable name;
    // * _FLOAT = Yes;
    // * int = Not a Variable name;
    // * FLOAT = Yes;
    // * You will succeed = Not a Variable name;

    return 0;
}
#include <iostream>
using namespace std;

// * Print Square of a number via taking from user.

int main()
{

    int x;
    cout<<"Enter the number : ";
    cin>>x; // User will give "x" a value.

    cout<<"Square of "<<x<<" "<<"is "<<x*x<<endl;

    return 0;
}